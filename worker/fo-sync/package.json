{
  "name": "fo-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Future Outfit cloud-save Worker (kid-safe code-based profiles).",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node test/sync.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
